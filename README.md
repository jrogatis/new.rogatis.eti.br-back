# rogatis.eti.br back end

## To run this app

- docker-compose up (load a mongo instance)
- npm i
- npm start.

## To run tests

- npm run test
- npm run coverage

the test enviroment was done with mongo in memory so a mongo isntance was not necessary

## Documentation

include a swagger file
